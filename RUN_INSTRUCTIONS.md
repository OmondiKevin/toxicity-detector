# Run Instructions

## Environment
- Python 3.10+
- `pip install -r requirements.txt`

## Quick Start
```bash
# From the project root after unzipping
pip install -r requirements.txt
streamlit run app/streamlit_app_multilabel.py
```

## Notes
- Models are expected in `models/`.
- Processed CSVs are expected under `data/processed/`.

